[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10.0"
requests = "^2.32.3"
splitwise = "^3.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
